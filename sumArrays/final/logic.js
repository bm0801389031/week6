/*function statement sum with parameter number*/
function sum(numbers) {
/*return reduce method with numbers as property a, b as parameter
fat arrrow function a + b, and 0 as its initial value*/
	return numbers.reduce((a, b) => a + b, 0);
}
/*declare function named stringToNumber with
parameter str*/
function stringToNumber(str){
/*return function parseInt() with parameter
str[ this method converts a string to num]*/
  return parseInt(str);
}
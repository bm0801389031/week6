//function statment reverseWords with parameter
// str
function reverseWords(str){
//return split method with str property quotes
// with no space as a parameter, then reverse
// method no parameter, then join method with
  // quotes and a space as parameter
  return (str.split(' ').reverse().join(' '))
}



//function named min with parameter list
var min = function(list){
//return Math.min method with parameter rest[because it is not
//a number were passing through] list
    return Math.min(...list)
}
//function named min with parameter list
var max = function(list){
//return Math.max method with parameter rest[becase it is not
//a number were passing through] list
    return Math.max(...list)
}